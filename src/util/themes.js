import { createTheme } from '@mui/material/styles';

export const buttonTheme = createTheme({
    palette: {
        primary: {
            main: '#2d3142',
            contrastText: '#ffffff',
        }
    },
})

export const threadButtonTheme = createTheme({
    palette: {
        primary: {
            main: '#505e74',
            contrastText: '#ffffff',
        }
    },
})